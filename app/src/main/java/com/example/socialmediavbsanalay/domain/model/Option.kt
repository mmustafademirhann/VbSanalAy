package com.example.socialmediavbsanalay.domain.model

data class Option(
    val iconResId: Int,       // Resource ID for the icon to represent the option
    val label: String,        // Label or name of the option (e.g., "Camera", "Gallery")
    //val actionType: OptionType // Enum or type to distinguish different actions (e.g., CAMERA, GALLERY)
)
